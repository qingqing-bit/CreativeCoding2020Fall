float x=400;
float y=400;

void setup() 
{
    size(800, 800);
    background(0);
    noStroke();
}

void draw() {
    int c = 0;
    for (int i = 1;i < 100;i++){
      c+=5;
      fill(c);
      ellipse(x,y,2500 / i,2500 / i);
    } 
}
